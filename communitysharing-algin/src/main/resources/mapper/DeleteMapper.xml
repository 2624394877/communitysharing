<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.taoxin.communitysharing.algin.domain.mapper.DeleteMapper">
    <delete id="batchDeleteDataAlignFollowingCountTempTable" parameterType="list">
        delete from `t_data_align_following_count_temp_${tableNameSuffix}` where user_id in
        <foreach open="(" item="userId" close=")" collection="userIds" separator=",">
            #{userId}
        </foreach>
    </delete>
    <delete id="batchDeleteDataAlignLikeCountTempTable" parameterType="list">
        delete from `t_data_align_content_like_count_temp_${tableNameSuffix}` where content_id in
        <foreach open="(" item="contentId" close=")" collection="contentIds" separator=",">
            #{contentId}
        </foreach>
    </delete>
    <delete id="batchDeleteDataAlignCollectCountTempTable" parameterType="list">
        delete from `t_data_align_content_collect_count_temp_${tableNameSuffix}` where content_id in
        <foreach open="(" item="contentId" close=")" collection="contentIds" separator=",">
            #{contentId}
        </foreach>
    </delete>
    <delete id="batchDeleteDataAlignPublishCountTempTable" parameterType="list">
        delete from `t_data_align_content_publish_count_temp_${tableNameSuffix}` where user_id in
        <foreach open="(" item="userId" close=")" collection="creatorIds" separator=",">
            #{userId}
        </foreach>
    </delete>
    <delete id="batchDeleteDataAlignFansCountTempTable" parameterType="list">
        delete from `t_data_align_fans_count_temp_${tableNameSuffix}` where user_id in
        <foreach open="(" item="userId" close=")" collection="userIds" separator=",">
            #{userId}
        </foreach>
    </delete>
    <delete id="batchDeleteDataAlignUserLikeCountTempTable" parameterType="list">
        delete from `t_data_align_user_like_count_temp_${tableNameSuffix}` where user_id in
        <foreach open="(" item="userId" close=")" collection="userIds" separator=",">
            #{userId}
        </foreach>
    </delete>
    <delete id="batchDeleteDataAlignUserCollectCountTempTable" parameterType="list">
        delete from `t_data_align_user_collect_count_temp_${tableNameSuffix}` where user_id in
        <foreach open="(" item="userId" close=")" collection="userIds" separator=",">
            #{userId}
        </foreach>
    </delete>
</mapper>