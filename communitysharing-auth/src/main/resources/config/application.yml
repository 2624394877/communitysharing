server:
  port: 8080
spring:
  profiles:
    active: dev

#mybatis:
#  mapper-locations: classpath:/mapper/*.xml 配置mybatis的xml文件位置 (classpath:/mapper/*.xml)表示扫描classpath下的mapper目录下的所有xml文件


############## Sa-Token 配置 (文档: https://sa-token.cc) ##############
sa-token:
  # token 名称（同时也是 cookie 名称）
  token-name: Authorization
  # token 前缀
  token-prefix: Bearer
  # token 有效期（单位：秒） 默认30天，-1 代表永久有效
  timeout: 604800
  # token 最低活跃频率（单位：秒），如果 token 超过此时间没有访问系统就会被冻结，默认-1 代表不限制，永不冻结
  active-timeout: -1
  # 是否允许同一账号多地同时登录 （为 true 时允许一起登录, 为 false 时新登录挤掉旧登录）
  is-concurrent: false
  # 在多人登录同一账号时，是否共用一个 token （为 true 时所有登录共用一个 token, 为 false 时每次登录新建一个 token）
  is-share: true
  # token 风格（默认可取值：uuid、simple-uuid、random-32、random-64、random-128、tik）
  token-style: random-128
  # 是否输出操作日志
  is-log: true

#alarm:
#  type: sms # 告警方式

#nacos: # 单体项目配置
#  config:
#    access-key: # nacos 密钥
#    secret-key: # nacos 密钥
#    data-id: communitysharing-auth # 配置数据ID
#    group: DEFAULT_GROUP # 配置分组
#    type: yaml # 配置文件类型
#    server-addr: 127.0.0.1:8848 # nacos 配置中心地址
#    auto-refresh: true # 是否自动刷新
#    remote-first: true # 是否优先从远程配置中心获取配置
#    bootstrap:
#      enable: true # 启动时是否加载配置

